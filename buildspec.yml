version: 0.1
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - ldconfig -p | grep libfontconfig
      - npm install
  pre_build:
    commands:
      - npm test
  build:
    commands:
      - npm run deploy
artifacts:
  type: zip
  files:
    - serverless-output.yaml
